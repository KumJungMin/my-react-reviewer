interface:
  display_name: "React Workflow Orchestrator"
  short_description: "Coordinate React requirements, batches, and review"
  default_prompt: "Use $react-workflow-orchestrator to map requirements, plan clear React/TypeScript work units, implement in commit-sized batches, and run final review."
