interface:
  display_name: "React AI Reviewer"
  short_description: "Final-review React code with multi-reviewer context"
  default_prompt: "Use $react-ai-reviewer to run a final review of this React code, preserving the original reviewer selection and context flow when helpful."
